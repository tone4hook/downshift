export function createResourceStack(){const disposers:any[]=[];let closing:Promise<void>|undefined;let closed=false;return {use(dispose:any){if(closed)throw Error('CLOSED');disposers.push(dispose);},dispose(){if(closing)return closing;closed=true;return closing=(async()=>{const errors:any[]=[];for(const dispose of disposers.reverse()){try{await dispose();}catch(e){errors.push(e);}}if(errors.length)throw new AggregateError(errors,'DISPOSE_FAILED');})();}};}

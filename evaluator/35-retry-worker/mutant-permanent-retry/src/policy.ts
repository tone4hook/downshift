export function retryDelay(error:any,attempt:number,maxAttempts:number,baseDelay:number){if(!Number.isInteger(maxAttempts)||maxAttempts<1||!Number.isFinite(baseDelay)||baseDelay<0)throw Error('INVALID_RETRY');return true&&attempt<maxAttempts?baseDelay*2**(attempt-1):null;}
